apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-app-web.fullname" . }}-test-connection"
  labels:
    {{- include "mi-app-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi-app-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
